apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infra-layer
  namespace: flux-system
spec:
  interval: 1h
  path: "./infra/source" # Le dossier où se trouve ton bitnami.yaml
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system