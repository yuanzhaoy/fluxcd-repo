apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: weavework-hr
  namespace: flux-system
spec:
  interval: 1m0s
  chart:
    spec:
      chart: ./charts/gitops-server
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: GitRepository
        name: flux-system
  values:
    deployment:
      additionalContainers: [ ] # Gardez-le si déjà présent
      securityContext:
        capabilities:
          drop:
            - NET_RAW
    adminUser:
      create: true
      passwordHash: $2a$12$XStXAMW19LmvcT3sDOdaEOw9r4IhpZoZIkhBKr6HYUEJdKaTPsqHy
      username: admin
    service:
      create: true
      type: ClusterIP
      port:
        https: 9001